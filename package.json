{
  "name": "sandcastle-demos",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "publish": "npm install && npm run build && pm2 start ./pm2.prod.json"
  },
  "dependencies": {
    "sanddunes": "0.0.1003",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
